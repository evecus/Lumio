/**
 * 设计画布尺寸
 *
 * 全 app 的界面都是按这个固定分辨率编写像素值的（对应之前反馈
 * “现在编译的 apk 在 1920x1080 的设备上显示最好”）。
 *
 * <ScaledStage> 会把这块画布整体等比缩放去适应真实设备屏幕，
 * 多余空间留白、居中显示，从而在任意分辨率/宽高比的设备上都能
 * 完整显示、不变形、不过大也不过小。
 *
 * 如果以后要整体调整设计基准（比如换成 4K 设计稿），只需要改这里，
 * 不需要改任何页面代码。
 */
export const CANVAS_WIDTH = 1280
export const CANVAS_HEIGHT = 720
